jobs:
  - name: old-min-sh
    type: runSh
    steps:
      - TASK:
        - script: echo "hello world"
      
  - name: new-min-sh-windows
    type: runSh
    runtime:
      nodePool: default_node_pool
      container: false
    steps:
      - TASK:
          name: echo-facts
          script: 
            - echo "cats are cute"
